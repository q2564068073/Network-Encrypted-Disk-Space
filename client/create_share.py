


# 创建共享空间函数
def create_shared_space(username,space_name):
    message = "create_shared_space|" + username + "|" + space_name
    return message

# 将用户添加到共享空间函数
def add_user_to_shared_space(space_name,username):
    message = "add_user_to_shared_space|" + space_name + "|" + username
    return message


